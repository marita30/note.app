# note.app 
## Instalacion 🔧
* Instalar NODE.
```
En la terminal , correr:  node -v si este comando no funciona probar con este brew install node.

```
* Instalar NPM.
```
* En la terminal correr:  npm install pil, luego, npm init y seguir las instruciones que salen en la terminal y eso te crea un archivo llamado Package.json
* Para instalar un paquete en el proyecto por ejemplo llamado un paquete Hapi,  seria-----> npm install hapi --save (save le dice a npm que instale el paquete dentro de una carpeta llamada "node_modules' y se actualizara su package.json.
* Instalar un paquete que sea necesario para cualquiera que este desarollando su aplicacion por ejemplo jasmine-node, seria-------> npm install jasmine-node --save-dev.

```
* HTTP-SERVER.
```
Correr el comando----> npm install http-server --save. y luego para correr el servidor----> node node_modules/http-server/bin/http-server.

```
